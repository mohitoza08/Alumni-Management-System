
  # Alumni Connect Platform Prototype

  This is a code bundle for Alumni Connect Platform Prototype. The original project is available at https://www.figma.com/design/PDUzRcHUA6m3APkf473FEm/Alumni-Connect-Platform-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  